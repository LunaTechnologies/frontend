import React from 'react';
import {SafeAreaView, View, Text} from 'react-native';

const Catalog = () => {
  return (
    <SafeAreaView>
      <Text>Catalog</Text>
    </SafeAreaView>
  );
};

export default Catalog;
