import React from 'react';
import { SafeAreaView } from 'react-native';
import Settings from './src/views/Settings';

const App = () => {
  return (
    <SafeAreaView>
      <Settings />
    </SafeAreaView>
  );
};

export default App;
